Proof-of-Concept for minimal-code-size CPace25519 as in draft-irtf-cfrg-cpace-18.

Released to the public domain under the CC0 license (full text in the 
tweetnacl_cpace25519.c source code file).

This library extends the tweetnacl library by what is needed for CPace.

Howto run the "testsuite".

gcc -o test.exe tweetnacl_cpace25519.c tests_tweetnacl_cpace25519.c -DCPACE_IR -DCPACE_SYM
./test.exe


In your application include the tweetnacl_cpace25519.h Header and add the single file
tweetnacl_cpace25519.c to your build Environment. Define either of 
the -DCPACE_IR -DCPACE_SYM compile switches depending on your use of CPace (ordered
Messages with clear initiator and responder or symmetric instantiation).
